{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/dmaynor/cheddar_framework/schemas/cheddar_track.schema.json",
  "title": "Cheddar Track",
  "description": "Captures reproducible problems, hypotheses, and candidate improvements",
  "type": "object",

  "properties": {
    "level": { "type": "string", "const": "cheddar_track" },
    "id": { "type": "string", "pattern": "^track_[a-z0-9_]+_v[0-9]+$" },
    "title": { "type": "string", "minLength": 1 },
    "supports_upper_layer": { "type": "string", "pattern": "^flow_[a-z0-9_]+_v[0-9]+$" },
    "issue": { "type": "string", "minLength": 1 },
    "hypotheses": { "type": "array", "items": { "type": "string", "minLength": 1 } },
    "repro_steps": { "type": "array", "items": { "type": "string", "minLength": 1 } },
    "enables_lower_layer": { "type": "array", "items": { "type": "string", "minLength": 1 } },
    "upward_feedback": { "type": "string" },
    "cheddar_state": { "type": "string", "enum": ["active","resolved","stinky"] },
    "lineage": {
      "type": "object",
      "properties": {
        "upstream_hash": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
        "hash": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" },
        "signed_by": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["upstream_hash","hash","timestamp"],
      "additionalProperties": false
    }
  },

  "required": ["level","id","title","supports_upper_layer","issue","lineage"],
  "additionalProperties": false
}
